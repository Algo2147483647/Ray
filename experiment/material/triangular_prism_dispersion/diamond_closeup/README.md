# Diamond close-up

An independent jewellery portrait scene using the parent experiment's generated
round-brilliant moissanite model. The stone is centred and lit only with white
emitters so visible colour comes from spectral dispersion inside the gem.

Run:

```cmd
run.cmd
```

Quick preview:

```cmd
run.cmd --width 256 --height 256 --samples 20
```
