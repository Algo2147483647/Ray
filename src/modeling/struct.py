import math
import numpy as np
from typing import List, Tuple, Union

# Define a type alias for points and triangles
Point = np.ndarray  # A point represented as a NumPy array [x, y, z]
Triangle = Tuple[Point, Point, Point]
